# Uvicorn entrypoint — the ASGI application object that uvicorn loads.
# Usage: uvicorn {{ package_name }}.asgi:application --host 0.0.0.0 --port 8000
from {{ package_name }}.core.asgi import create_app

application = create_app()
